# Information Chemistry v0.910 — Standalone Constructive Composition Quotient

Run after clean extraction:

```bash
python run_all_tests_v0_910.py
```

Requirements: Python standard library only. Network: not required.

The ZIP contains the complete v0.910 DAG and rule registry plus recursive immutable anchors for v0.901, v0.900, v0.1, and the root source snapshot.
