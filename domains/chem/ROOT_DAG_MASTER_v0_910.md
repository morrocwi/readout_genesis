# Root DAG Master v0.910

**Standalone rule:** this file contains the complete DAG, not a pointer to v0.901. The byte-complete v0.901 release is also embedded under `anchor_v0_901/`.

```mermaid
flowchart TD
    R0["R0 retained distinction"] --> R1["R1 identity/history/lineage/context"]
    R1 --> R2["R2 typed state + boundary"]
    R2 --> R3["R3 quotient sufficiency/refinement"]
    R2 --> R4["R4 ledger map"]
    R4 --> R5["R5 closed-boundary conservation"]
    R5 --> R6["R6 preserving kernel"]
    R6 --> R7["R7 extent coordinates"]
    R7 --> R8["R8 positivity/capacity"]
    R2 --> R9["R9 conditional additive readout"]
    R3 --> R10["R10 stationarity hierarchy"]

    R2 --> P2["P2 frozen generator/profile card"]
    P2 --> R11["R11 typed generator registry"]
    R11 --> R12["R12 free structural carrier N^G"]
    R12 --> R13["R13 occupation homomorphism"]
    R3 --> R14["R14 permutation sufficiency gate"]
    R13 --> R14
    P2 --> R14
    R11 --> R15["R15 conditional unique decomposition"]
    R12 --> R15
    R11 --> R16["R16 refinement/coarsening commute"]
    R12 --> R16
    R13 --> R17["R17 marked quotient + lineage sidecar"]
    R14 --> R17
    R15 --> R17
    R16 --> R17
    R17 --> R4
    R17 --> R9

    T0["T0 empirical tape"] --> D0["D0 candidate-law discovery"]
    P0["P0 discovery grammar"] --> D0
    P1["P1 complexity/tie protocol"] --> D0
    R3 --> D0
    D0 --> D1["D1 holdout/intervention/transport"]
    D1 --> O0["O0 calibrated readout"]
    D1 --> N0["N0 certified numerical method"]
    R8 --> N0
    R9 --> O0
    O0 --> C0["C0 bounded claim"]
    N0 --> C0
    R10 --> C0
```

## v0.910 non-drift rule

The mathematical carrier \(\mathbb N^G\) is commutative by construction. **The source domain is not declared commutative for free.** A source-to-count quotient is admitted only when every same-count pair has identical registered readout and successor signatures under every frozen context/intervention profile.

## Structural versus marked state

- Structural projection: occupation vector \(c\in\mathbb N^G\).
- Marked state: \((c,\lambda)\), where \(\lambda\) retains append-only lineage.
- Two histories may share structural projection while remaining distinct marked states.
