<!-- Knowledge-graph map of content. One root; every node below is a readout of it. -->

# 🌱 Readout Genesis

> **The universe is retained before it is divided into domains.**
> One root — *information* — read out step by step. "Physics", "chemistry", "biology", "mind"
> are not the base; they are *translations* of one retained structure under different questions.

**Claim discipline (read first):** *readout‑not‑truth.* Verified structure is not physical truth.
Every claim carries a tier and never leaves it:

| tier | meaning |
|---|---|
| `Th_coqc` | machine‑checked, axiom‑free (over ℚ) |
| `finite_diagnostic` | measured / computed, finite |
| `Dr` | declared bridge / human narrative |
| `Open` | not established |

There is no hidden ℝ behind the odometer. What is real is the finite, discrete readout an
agency operator returns.

---

## The one root

```
                         δ_R = (a ♯ b)            one retained distinction
                              │
                              ▼
      L_R = D_W − W   ── the graph Laplacian: the ONE genuinely derived link  [Th_coqc]
                              │
                              ▼
   discrete stepper (MQ.08)  ──►  the universal spine is its coarse‑grain readout, not the reverse
                              │
                              ▼
        M ∂²Φ + D ∂Φ + K·L_R Φ + ∇V(Φ) = J − η        (the trunk; SI enters only as an adapter)
```

Everything else is a **readout** of that one operator.

---

## The three layers (the whole canon is organized this way)

```
        ┌───────────────────────────────────────────────────────────────┐
        │  ONTOLOGICAL   retention · state · lineage · tape               │
        │       │        (δ_R → spine → gateway; truth is retained first) │
        │       ▼                                                         │
        │  TRANSLATIONAL  a domain = the minimal sufficient,              │
        │       │         dynamically‑closed translation q_α of the       │
        │       │         retained state. A bridge is real only if it     │
        │       │         COMMUTES:  T·F♯_a = F♯_b·T  and preserves readout│
        │       ▼                                                         │
        │  EPISTEMIC     Registrar → Maker → Freeze → Checker → Auditor   │
        │                (the answer must never leak back into inference) │
        └───────────────────────────────────────────────────────────────┘
```

- **field‑count ≠ domain‑count** — a many‑field system can be one domain if the coupling is
  needed for one readout; a single retained field can read out as many domains. The
  "how many fields?" question is a *sufficiency/closure* question, not a counting one.
- **forbidden order** (the Buckingham trap): `textbook‑name → stuff parameters → declare derived`.
  **correct order:** `Retention → Structure → Domain‑translation → Meaning → Report`.

---

## Node map

```
                     [ READOUT GENESIS CANON ]
                              │
   ┌───────────────┬─────────┼──────────────┬────────────────────┐
   ▼               ▼         ▼              ▼                    ▼
 root axioms   the trunk   twelve faces   τ_c scale bus      domain leaves
 (δ_R, L_R)    equation    of the spine   (τ_c = M/D,        (each a q_α:
                                          mass is a          quantum · geometry
                                          readout of τ_c)    · biology · social ·
                                                             cognition · … many TBD)
   │               │            │              │                    │
   └───────────────┴─────┬──────┴──────────────┴────────────────────┘
                         ▼
         epistemic core · Maker–Checker firewall · claim discipline · non‑claims
```

**Honest edges (what is NOT settled):**
- `M` is **posited**, not derived (forcing it from the root failed repeatedly); mass is a readout of `τ_c`.
- Several domain bridges are **blank/TBD** — recorded, never fabricated. A *failing* bridge
  (e.g. a single quantum‑only quotient that does not commute to biology) is documented as a feature.
- Turbulence lives in the nonlinear `∇V` term, not the linear `M·a`.

---

## Files

- **`READOUT_GENESIS_CORE.md`** — the core canon (single file; more nodes will be split out later).

*More files will be added deliberately. Update knowledge in the core, in one place.*
